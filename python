n=int(input())
start_num=0
for num in range(1,n+1):
    start_num=start_num+num
for i in range(n):
    each_row=""
    left_space=" "*(2*i)
    for column in range(n-i):
        each_row=each_row+str(start_num)+" "
        start_num=start_num-1
    x=left_space+each_row
    print(x)
